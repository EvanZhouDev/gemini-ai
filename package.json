{
	"name": "gemini-ai",
	"version": "1.0.0",
	"author": "",
	"main": "index.js",
	"description": "The easiest way to access Google's Gemini model",
	"license": "ISC",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"type": "module",
	"devDependencies": {
		"test": "jest"
	}
}
